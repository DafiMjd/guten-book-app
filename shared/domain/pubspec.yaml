name: domain
description: "A new Flutter module project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  component:
    path: ../component
  dependencies:
    path: ../dependencies
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
